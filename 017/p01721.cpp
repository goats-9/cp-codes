#include <bits/stdc++.h>
using namespace std;

int main() {
    int x_max, y_max, a, b, c, d;
    string cmd;
    while(getline(cin, x)) {
        if (x[0] >= '0' && x[0] <= '9') 
    }
    return 0;
}